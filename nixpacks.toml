[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare yarn@4.12.0 --activate",
  "yarn install --frozen-lockfile",
]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start:prod"
